apiVersion: nominatim.zebernst.dev/v1alpha1
kind: NominatimOperation
metadata:
  labels:
    app.kubernetes.io/name: nominatim-operator
    app.kubernetes.io/managed-by: kustomize
  name: nominatimoperation-sample
spec:
  # Imperative finite workflow — do NOT manage via Flux/GitOps Kustomizations.
  type: Update
  nominatimInstanceRef:
    name: nominatim-sample
  # Optional: regions for AddRegions / Rebuild
  # regions:
  #   - north-america/us-west
  # Optional: override Nominatim.spec.staging for this operation's PVC
  # staging:
  #   size: 50Gi
  #   storageClassName: fast
