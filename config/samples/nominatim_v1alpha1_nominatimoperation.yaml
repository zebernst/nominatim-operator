apiVersion: nominatim.zebernst.dev/v1alpha1
kind: NominatimOperation
metadata:
  labels:
    app.kubernetes.io/name: nominatim-operator
    app.kubernetes.io/managed-by: kustomize
  name: nominatimoperation-sample
spec:
  # Finite workflow Job — do NOT manage via Flux/GitOps. Prefer letting the
  # NominatimInstance controller create Operations; use kubectl only for one-offs.
  # See docs/operations.md and docs/concepts.md.
  type: Update
  nominatimInstanceRef:
    name: nominatim-sample
  # regions:  # AddRegions / Rebuild
  #   - north-america/us-west
  # staging:  # optional PVC override for this Operation
  #   size: 50Gi
  #   storageClassName: fast
